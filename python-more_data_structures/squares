# Different methods to create a list according to a formula
squares = []
for x in range(10):
    squares.append(x**2)

# Or

squares = list(map(lambda x: x**2, range(10)))

# or

squares = [x**2 for x in range(10)]

print(squares)